{% extends 'dashboard/index.html.twig' %}

{% block content %}
    <div class="p-8 bg-gray-900 min-h-screen flex flex-col">
        <h1 class="text-2xl font-bold mb-4 text-yellow-400">Modifier l'annonce</h1>

        <div class="bg-gray-800 p-6 rounded-lg shadow-lg">
            {{ include('pure_annonce/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>

        <div class="mt-6">
            <a href="{{ path('annonce_index') }}" class="text-yellow-400 hover:text-yellow-300">Retour à la liste</a>
        </div>

        <div class="mt-6">
            {{ include('pure_annonce/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
