<form method="post" action="{{ path('annonce_delete', {'id': pure_annonce.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pure_annonce.id) }}">
    <button class="btn">Delete</button>
</form>
