{% extends 'base.html.twig' %}

{% block title %}PureStatut{% endblock %}

{% block body %}
    <h1>PureStatut</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pure_statut.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ pure_statut.intitule }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pure_statut_index') }}">back to list</a>

    <a href="{{ path('app_pure_statut_edit', {'id': pure_statut.id}) }}">edit</a>

    {{ include('pure_statut/_delete_form.html.twig') }}
{% endblock %}
