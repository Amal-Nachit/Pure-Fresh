{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="mt-4 inline-block bg-lime-600 text-white font-semibold py-2 px-4 rounded-lg hover:bg-lime-700 transition duration-300 focus:outline-none focus:ring-2 focus:ring-lime-500 focus:ring-opacity-50">
        {{ button_label|default('Sauvegarder') }}
    </button>
{{ form_end(form) }}