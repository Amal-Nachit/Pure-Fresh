{% extends 'dashboard/vendeur.html.twig' %}

{% block content %}
    <div class="p-8 bg-gray-900 min-h-screen flex flex-col">
        <h1 class="text-2xl font-bold mb-4 text-yellow-400">Mettre à jour</h1>
        
        <div class="bg-gray-800 p-6 rounded-lg shadow-lg">
            {{ form_start(form) }}

            {% for field in form %}
            {% if field.vars.name != '_token' %}

                <div class="mb-4">
                    {{ form_label(field) }}
                    {{ form_widget(field, {'attr': {'class': 'w-full p-3 bg-gray-700 text-white rounded-lg shadow-md focus:ring-2 focus:ring-yellow-500 focus:outline-none'}}) }}
                    
                    {% if field.vars.errors|length > 0 %}
                        <div class="text-red-600">
                            {% for error in field.vars.errors %}
                                <p>{{ error.message }}</p>
                            {% endfor %}
                        </div>
                    {% endif %}
                </div>

            {% endif %}
            {% endfor %}

            <div class="mt-4">
                <button type="submit" class="px-5 py-3 bg-yellow-500 text-gray-900 rounded-lg shadow-md hover:bg-yellow-600 focus:outline-none focus:ring-2 focus:ring-yellow-700">
                    Mettre à jour
                </button>
            </div>

            {{ form_end(form) }}
        </div>
    </div>
    
{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('../assets/js/adresse.js') }}"></script>
{% endblock %}
{% endblock %}
