{% extends 'dashboard/vendeur.html.twig' %}

{% block content %}

    <h1 class="text-2xl font-bold mb-4 text-yellow-400">Modifier le produit</h1>

    {{ include('pure_produit/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('produit_index') }}">back to list</a>

    {{ include('pure_produit/_delete_form.html.twig') }}
{% endblock %}
