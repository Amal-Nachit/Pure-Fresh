<form method="post" action="{{ path('produit_delete', {'id': pure_produit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pure_produit.id) }}">
    <button class="btn">Delete</button>
</form>
