<form method="post" action="{{ path('commande_delete', {'id': pure_commande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pure_commande.id) }}">
    <button class="btn">Delete</button>
</form>
