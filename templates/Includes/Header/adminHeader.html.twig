<header class="bg-gray-800 p-4 shadow-md">
    <div class="container mx-auto flex justify-between items-center">
        <h2 class="text-2xl font-bold text-white">Tableau de bord</h2>
        <nav class="flex items-center">
            <ul class="flex space-x-6 mr-6">
                <li>
                    <a href="{{ path('admin_dashboard') }}" class="text-lg hover:text-lime-400 {{ app.request.get('_route') == 'admin_dashboard' ? 'text-lime-400 font-bold' : 'text-gray-200' }}">Annonces à approuver</a>
                </li>
                <li>
                    <a href="{{ path('user_index') }}" class="text-lg hover:text-lime-400 {{ app.request.get('_route') == 'user_index' ? 'text-lime-400 font-bold' : 'text-gray-200' }}">Utilisateurs</a>
                </li>
                <li>
                    <a href=" {{ path('admin_transactions') }}" class="text-lg hover:text-lime-400 {{ app.request.get('_route') == 'transactions' ? 'text-lime-400 font-bold' : 'text-gray-200' }}">Transactions</a>
                </li>
            </ul>
            <a href="{{ path('admin_logout') }}" class="bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-4 rounded transition duration-300 ease-in-out">
                Déconnexion
            </a>
        </nav>
    </div>
</header>